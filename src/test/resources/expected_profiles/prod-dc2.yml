application:
  name: config-data-test
  region: NAMR

spring:
  cloud:
    stream:
      bindings:
        consumer-in-0:
          group: consumer-group-production
          destination: consumer-destination-NAMR-production-dc2
